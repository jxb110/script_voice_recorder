# 脚本录音器：移动端界面设计

## 设计目标

本应用面向需要按指定文本采集语音的录音人员。界面按 **Android 竖屏 9:16** 与单手操作设计：主要动作固定在拇指易触达的屏幕下方，录制过程中只保留与当前句子相关的信息，避免误操作。数据默认保存在设备本地；不会要求用户登录，也不会上传录音。

## 屏幕清单

| 屏幕 | 主要内容 | 核心操作 |
|---|---|---|
| 项目主页 | 最近录音任务、进度、发音人、继续录制入口 | 新建任务、继续录制、进入项目管理 |
| 发音人选择 | 已保存发音人列表；名称、性别与年龄摘要 | 选择、编辑、新增、切换发音人 |
| 发音人编辑 | 名称输入框、性别分段控件、年龄数字输入框 | 保存或取消 |
| 脚本导入 | 从系统文件管理器选择 `.txt`、`.csv`、`.tsv` 或 JSON 脚本；解析结果预览 | 选择文件、确认导入、命名任务 |
| 脚本列表 | 一句一行的任务进度列表；录制状态、序号、朗读文本摘要 | 跳转到指定句、查看录制状态 |
| 录制工作台 | 提示词、带拼音字元的朗读文本、计时、波形、当前位置和底部主按钮 | 开始、停止、播放、重录、前后跳句 |
| 录音详情 | 已录句子清单、播放控制、完成进度 | 分享单句、跳转重录、分享完整录音包 |
| 录音设置 | 采样率、比特率、声道、首尾静音毫秒数 | 修改后保存到本地 |

## 主要内容与交互

录制工作台上方以小号、低对比度文字展示**提示词**，中部以大号、居中排版展示**朗读文本**。拼音脚本被解析成字元数组；每个汉字与其拼音在同一个垂直字元单元内排列，确保系统换行或字体变化时拼音仍与对应汉字绑定。普通文本则以同样的阅读字号呈现。

录制按钮位于底部安全区域上方，静止状态显示“开始录制”。点击后先申请并打开麦克风，进入“首端静音”倒计时；在设定的静音时长后才切换为红色“停止录制”和正式计时。点击停止后，界面先显示“尾端静音处理中”，维持静音时长，再落盘保存音频。保存成功后自动标记当前句子为“已录制”，并前进到下一句；如该句已有录音，则提示用户以新录音覆盖。

## 关键用户流程

| 用户目标 | 操作路径 |
|---|---|
| 开始一个新录音任务 | 项目主页 → 新建任务 → 填写或选择发音人 → 导入脚本 → 脚本预览 → 进入录制工作台 |
| 逐句录音 | 录制工作台 → 开始录制 → 首端静音结束 → 朗读并观察波形 → 停止录制 → 尾端静音结束 → 自动保存并前进 |
| 修正已录句子 | 录音详情或脚本列表 → 点选句子 → 播放确认 → 重录 → 保存覆盖 |
| 查看进度并跳转 | 录制工作台的句子定位器或脚本列表 → 输入/点选目标句 → 打开该句录制工作台 |
| 导出录音 | 录音详情 → 对单句点选分享；或选择“分享完整录音包” → 系统分享面板 |

## 颜色与视觉语言

应用使用偏专业采集工具的深靛蓝与暖白基调。主色为 **#2F4DA0**（操作与聚焦），录制态为 **#D64646**（明确但不过度刺眼），完成态为 **#2F855A**，警示为 **#B7791F**，页面背景为 **#F7F8FC**，卡片为 **#FFFFFF**，主文字为 **#182033**，辅助文字为 **#65708A**。圆角控制在 14–20px；录制按钮为满宽圆角胶囊，重要的已录/未录状态同时使用色彩、文字与图标区分，保证可访问性。

## 数据模型

| 实体 | 关键字段 | 本地保存位置 |
|---|---|---|
| 发音人 | `id`、`name`、`gender`、`age`、`createdAt` | AsyncStorage |
| 录音设置 | `sampleRate`、`bitRate`、`channels`、`leadingSilenceMs`、`trailingSilenceMs` | AsyncStorage |
| 脚本任务 | `id`、`name`、`sourceFileName`、`sentences`、`speakerId`、`createdAt` | AsyncStorage；源脚本副本位于应用的 `scripts/` 目录 |
| 句子 | `id`、`index`、`prompt`、`rawText`、`tokens`、`recordingUri`、`recordedAt` | AsyncStorage；音频文件位于以发音人信息命名的任务目录 |
| 拼音字元 | `char`、`pinyin` | 作为句子的 `tokens` 嵌入保存 |

## 脚本格式兼容策略

应用支持每行一个句子的纯文本，以及两列脚本（第一列为朗读文本、第二列为提示词）的 CSV/TSV 文件。JSON 可直接使用包含 `char`、`pinyin` 的字元对象数组，且允许在数组末尾放置 `{"Mark":"提示词"}`。如果导入文本不是有效的拼音 JSON，应用会以普通朗读文本处理，并在导入预览中标识解析结果。
