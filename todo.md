# Project TODO

- [x] 配置应用主题、名称及底部导航结构
- [x] 建立本地数据模型与 AsyncStorage 持久化层
- [x] 实现发音人新建、编辑、选择与切换
- [x] 实现从系统文件管理器选择脚本并复制至 `scripts/` 目录
- [x] 支持单个或批量选择脚本文件，并在 Android 中按目录合并读取脚本
- [x] 支持 TXT、CSV/TSV 和拼音 JSON 脚本解析及导入预览
- [x] 实现逐句列表、录制状态、句子跳转与任务进度
- [x] 实现提示词和拼音绑定字元的朗读文本展示
- [x] 实现采样率、比特率、声道及首尾静音设置
- [x] 实现麦克风权限、逐句录制、首尾静音与实时波形反馈
- [x] 实现录音落盘、自动前进、播放和单句重录
- [x] 实现单句音频分享与按发音人信息组织的整包分享
- [x] 生成并配置自定义应用图标
- [x] 编写测试并验证关键本地流程
- [x] 排查并恢复 Metro 预览服务响应
- [x] 优化 Metro 工作区与编译配置，避免内存终止
- [x] 让 CSV/TSV 第二列和 JSON `Mark` 内容准确显示为录制提示词
- [x] 将录音能量柱替换为连续的实时录制波形
- [x] 为提示词解析与波形组件补充回归验证
- [x] 为发音人信息填写与编辑页面提供明确的返回导航
- [x] 在任务列表与任务详情中支持二次确认后删除任务
- [x] 仅在任务尚无录音进度时支持更换录音文本并预览解析结果
- [x] 为任务删除与脚本更换保护规则补充回归验证
- [x] 删除任务时清理该任务位于应用目录内的脚本与录音文件
- [x] 修正 JSON 字元脚本解析，避免显示原始 JSON 内容
- [x] 确保每个 char 下方与对应 pinyin 上方严格绑定展示
- [x] 为多句 JSON 拼音脚本补充解析回归测试
- [x] 区分网页预览与 Android 原生的实时麦克风计量能力
- [x] 优化 Android 录音分贝归一化与连续波形采样反馈
- [x] 为波形数据转换补充回归验证
- [x] 排查 Honor 9X Pro（HarmonyOS 3.0）APK 启动闪退与原生配置兼容性
- [x] 修复高风险启动配置并提供设备端崩溃日志采集指引
- [x] 将脚本选择限制为 TXT，并移除 CSV、TSV 与独立 JSON 文件导入
- [x] 将 TXT 每行解析为 JSON 字元数组，映射 char、pinyin 与 Mark
- [x] 更新导入提示并验证多行 TXT JSON 脚本
- [x] 排查 Android Gradle 发布构建失败的原生配置与依赖风险
- [x] 修复可确定的 Android 构建配置问题并验证 Expo 配置
- [x] 提供 Run gradlew 阶段日志的精确采集与提交指引
- [x] 对齐 Expo SDK 54 所需的 document-picker、file-system、sharing 与 asset 原生依赖版本
- [x] 消除 expo-file-system 重复原生模块并重新运行 Expo 依赖诊断
- [x] 完成 expo-asset 安装并复核 Expo SDK 54 原生依赖锁定结果
- [x] 修复 Reanimated 强制要求启用新架构导致的 Android Gradle 构建失败
- [x] 验证新架构 Expo 配置与 APK 发布前检查
- [x] 将录制与播放改为可完整查看的音频波形，并显示播放进度
- [x] 将录制主按钮固定在录制页面底部安全区域
- [x] 修复录制页面点击左上角关闭导致的应用闪退
- [x] 多 TXT 文件导入后要求用户选择单一脚本创建任务，不再合并文本
- [x] 为波形序列、关闭导航和多脚本选择补充回归验证
- [x] 建立中文与英文文案资源，并按系统语言自动选择
- [x] 将主要任务、导入、录制、设置和分享界面接入本地化文案
- [x] 为系统语言识别与英文回退策略补充回归验证
- [x] 将录音自动导出到手机可见的 record_jxb/wave 公共目录
- [x] 按发音人信息与任务名分层组织公共音频文件
- [x] 在公共目录写入失败时保留应用内录音并提示用户
- [x] 修复 ExpoMediaLibrary 无法复制私有音频到外部存储的问题
- [x] 改用 Android 媒体库 record_jxb/wave 目录作为公共音频导出目标
- [x] 重做紧凑、居中且更美观的完整音频波形视觉
- [x] 增大朗读文本卡片高度与页面占比，保持文字字号并将提示改为“请朗读上方文本”
- [x] 在发音人列表与编辑页提供二次确认删除，并保护关联任务
- [x] 为公共导出副本与波形样式数据转换补充回归验证
- [x] 将提示词和朗读文本标签固定在各自文本框左上角
- [x] 在朗读文本框右上角加入字号设置按钮与实时滑条
- [x] 持久化朗读文字号并补充设置回归验证
- [x] 支持朗读文本过长时在卡片内上下滑动，保持顶部标签和字号按钮可见
- [x] 重绘波形以显著区分静音、有声和首尾静音区间
- [x] 修复录制期间点击左上角关闭导致的应用闪退
- [x] 在全部句子录制完成后显示完成任务按钮并退出任务
- [x] 为每句提示词加入从左到右的水波式强调动画
- [x] 为波形分区、关闭稳定性和任务完成流程补充回归验证
- [x] 在完成任务状态下保留当前句的重新录制入口
- [x] 仅在相邻句提示词内容变化时播放水波强调动画
- [x] 更新 README，说明应用功能、脚本格式、录制流程和本地目录
- [x] 将 record01.txt 与 record02.txt 加入仓库示例脚本目录
- [x] 提交并同步当前代码、文档和示例脚本到 GitHub
- [x] 在设置页底部显示应用版本号和“Powered by 基因不投降”署名
- [x] 将设置页更新及全部最新代码提交并推送到 GitHub
- [x] 将录音输出改为 WAV，并在设置页仅提供 16-bit 与 32-bit 位深选择
- [x] 重新设计设置页，将保存录音设置固定在顶部便于即时保存
- [x] 用“文件快传”替代目录读取 TXT 脚本入口，支持局域网读写脚本与已完成录音
- [x] 增大提示词水波强调动画的移动、放大和高光对比度
- [x] 验证录音设置、提示词动效与文件快传流程，并同步到 GitHub
- [x] 采用原生构建方案，支持前台局域网文件快传与重新安装 APK
- [x] 编写 Windows 本地 Android Studio APK 构建与安装指南，绕过 EAS 配额
- [x] 修复文件快传局域网地址被浏览器拒绝连接的问题
- [x] 为文件快传补充可验证的服务监听状态、端口和故障提示
- [x] 将录制页的 Sentence 跳转输入改为点击选择句子列表
- [x] 参考 GSYRecordWave 重绘录制和播放波形，并严格限制振幅不越出边界
- [x] 点击上一句、下一句或重新录制时立即停止当前播放并重置进度
- [x] 替换不稳定的文件快传监听实现，确保局域网浏览器可获得 HTTP 响应
- [x] 参考 RemoteFileManager 优化文件快传的局域网服务与浏览器端文件管理体验
- [x] 将设置页重设为单屏展示并去除上下滑动
- [x] 修改配置但未保存时，离开或切换页面前提示保存或放弃
- [x] 将应用正式版本号和设置页版本展示升级为 2.0.0
- [x] 优化英语 Tasks 页顶部布局，确保长副标题下右上角图标不越出屏幕
- [x] 补齐 Tasks、Scripts、任务详情、创建任务和发音人页面的英语本地化
- [x] 将提示词水波强调高光由蓝色改为鲜红色
- [x] 调整首尾静音时序：点击录制立即开麦，尾静音结束后再闭麦保存
- [x] 在首端静音、有效录制和尾端静音阶段持续采样并绘制麦克风波形
- [x] 修正设置页版本显示为 2.0.0，并重排版式填充单屏空间
- [x] 在 Scripts 页 TXT JSON 格式说明下展示可复制的示例行
- [x] 每轮修改完成并通过验证后提交并推送最新代码到 GitHub
- [x] 以“管理所有文件”权限将文件快传升级为可浏览共享存储、上传下载、建目录和删除文件的局域网文件管理器
- [x] 在文件快传页面显示 record_jxb/wave 录音导出目录，并默认从该目录进入
- [x] 强化首端和尾端静音输入区域的视觉对比与可编辑性
- [x] 在当前录制配置摘要中显示首端和尾端静音时长
- [x] 修复 Android 共享存储 record_jxb/wave 目录创建被拒绝的问题
- [x] 修复设置页当前配置卡片超出背景框的单屏布局问题
- [x] 统一录音导出和文件快传为同一 record_jxb/wave 公共根目录
- [x] 收窄设置页首端和尾端静音输入框，保留清晰数值与单位显示
- [x] 为首端和尾端静音输入框加入上下微调按钮
- [x] 参考 RemoteFileManager 修复电脑端无法访问文件快传地址的服务生命周期与 HTTP 响应链路
- [x] 修复电脑端可访问但目录读取、上传和下载无效的文件快传接口
- [x] 移除文件管理页冗余的“导入录音脚本”区域
- [x] 支持浏览器多选文件并显示批量上传进度与结果
- [x] 支持将目录及全部子目录内容递归打包下载
- [x] 移除文件夹下载的 40MB 内存限制，改为临时 ZIP 文件与流式传输
- [x] 重绘波形采样映射和相邻线条样式，增强真实感、振幅层次与边界内区分度
- [x] 修复浏览器多选上传仅保存最后一个文件的问题
- [x] 移除手机端文件快传页面的已接收脚本与空脚本提示区域
- [x] 支持在电脑浏览器文件管理页拖拽多个文件进行批量上传
- [x] 在新建录音任务页发音人选择区域旁增加创建发音人入口
- [x] 将朗读文本默认字号设为最小值
- [x] 将录音默认参数设为首尾静音 500ms、16-bit 与单通道
- [x] 生成科技人工智能风格的新应用图标并替换全平台资源
- [x] 生成更扁平的 iOS 风格科技人工智能图标并压缩替换全平台资源
- [x] 修复大文件上传并支持稳定的分块写入
- [x] 将当前目录区域作为多文件拖拽上传目标
- [x] 将录音默认采样率设为 48kHz
- [x] 在不改动功能逻辑前提下，将页面、卡片、按钮、导航、弹窗和图标统一重构为 iOS 液态玻璃视觉
- [x] 为底部任务、脚本与设置导航加入切换时闪烁、气泡与轻微摆动的液态玻璃动效
- [x] 增强底部导航切换气泡的尺寸、对比度、上浮距离与持续时间，确保真机上清晰可见
- [x] 实现 3.0.0 离线局域网多设备同步录音基础：主控房间、客户端加入、双端设备上线离线与录音状态监控、异常提示和定时控制指令
- [x] 参考 Android Liquid Glass 公开示例，重构玻璃按钮、开关、滑条和底部导航的折射、高光与流体动效，且不改变业务逻辑
- [x] 修复同一局域网下加入同步房间失败的问题，并将主控地址输入简化为 IP 与端口
- [x] 修复加入失败后仍误显示“已连接主控”的状态残留，仅在主控欢迎握手成功后进入连接态
- [x] 将同步房间面板改为可折叠、压缩顶部进度区，并在主控或客户端同步模式下隐藏本机开始录制按钮与句子清单
- [x] 缩小设置保存图标并统一为任务页圆形图标风格，修复小屏底部导航标签与新建任务按钮文字裁切或重叠
- [x] 为主控同步房间展示可放大的二维码，并为加入已有房间增加扫码填充 IP、端口和口令
- [x] 让同步房间模块进入任务详情时默认折叠且始终可展开，创建或加入房间后继续保留折叠控制并修正提示位置
- [x] 点击加入已有房间进入连接流程时即隐藏本机开始录制与句子清单，并进一步压缩任务详情顶部进度摘要
- [x] 将设置保存按钮改为蓝色填充和白色确认图标，提升未保存设置时的可见性
- [x] 参考 OpenCamera-Sensors 排查并修复 Android 真机局域网主控监听、端口可达性和客户端握手失败问题
- [x] 参考 SoundSynk 的设备发现、连接建立、时钟校准与控制分发架构，重构多设备同步连接链路
- [ ] 修复真机原生 TCP 的数组、Buffer 与数字键对象握手响应解码，避免将 HTTP 响应误报为 400 Bad Request
- [ ] 以两台真机能够创建房间、加入房间并执行主控录制指令为多设备同步录制的核心验收标准
- [ ] 将主控请求格式错误、端口未监听、房间或任务不匹配等握手失败原因分别显示，直至两台真机成功建立会话
- [ ] 修复主控将 Android 原生 TCP 升级请求误判为缺少 WebSocket 信息并返回 400 的问题，兼容本地受控协议握手
- [ ] 修复 Android 原生 TCP 首包被主控判定为 malformed request line 的写入编码和服务端解析问题
- [x] 关闭主控或客户端同步后自动折叠同步模块，并为客户端补充关闭同步入口
- [x] 进一步压缩任务详情的任务名称、发音人和进度摘要模块，恢复字号滑条可拖动交互并增强底部导航液态玻璃选中反馈
- [x] 将字号滑条改为高对比度、更大且可拖动的深色滑块，并降低底部导航高度
- [x] 重排当前录制配置摘要，使首端和尾端静音在窄屏设备上完整、明确显示
- [ ] 参考 SoundSynk 的会话、连接和同步控制分层继续完善本地多设备同步录制实现
- [x] 仅在至少一台客户端成功加入且在线时允许主控进入同步录制，并显示未满足条件的禁用提示
- [x] 参考 LiquidBottomTabs 的视觉交互重做底部导航为水滴附着、合并和滑移到选中图标的动效
- [x] 提高选中液滴的透明玻璃感并调整层级，确保任务、脚本和设置图标与标签不被遮挡
- [x] 以双端原生 TCP 房间行协议替换 Android 同步房间的 HTTP/WebSocket 升级步骤，并加入协议版本校验
- [ ] 在两台真机上验收原生 TCP 房间协议：创建房间、加入房间、设备在线与主控录制指令均成功
- [x] 让主控二维码携带同步任务键，客户端扫码后以该任务键加入房间，避免本地任务 ID 不同导致口令正确仍被拒绝
- [x] 记录主控和客户端的同步握手、任务键、口令校验与拒绝原因，并在面板提供复制和导出诊断日志入口
- [ ] 修复主控在确认原生 TCP 协议前关闭连接的问题，兼容首包分片并记录双方协议行与关闭原因
- [x] 归一化房间口令和同步任务键，分别显示口令不匹配或任务键不匹配，并在日志中记录二维码版本与键指纹
- [x] 清理失败或遗留的同步会话状态，避免其误判为已连接并阻止创建主控房间
- [ ] 获取同次主控与客户端的同步诊断日志，并以真实协议行、口令和任务键指纹定位加入失败根因
- [x] 创建主控前强制回收失效同步会话、Socket 与服务端资源，使应用无需后台重启即可重新建房
- [x] 在主控和客户端活动同步房间中始终显示关闭同步按钮，点击后释放资源并自动折叠同步模块
- [x] 关闭同步后立即清空任务详情的旧会话身份，避免再次创建主控时仍提示房间不匹配
- [x] 移除创建主控的旧房间冲突阻断，点击创建时无条件终止残留会话并创建新房间
- [x] 删除同步面板“当前任务与已连接房间不匹配”的阻断渲染分支，旧会话不再阻止创建主控
- [x] 修复主控已创建并监听后同步面板未刷新展示房间地址、口令、二维码、设备列表和关闭按钮的问题
- [x] 同任务主控已监听时复用现有房间，阻止重复点击或状态触发重置房间口令与二维码
- [x] 建房成功后强制发布同步状态并在面板即时渲染主控地址、端口、口令和二维码
- [x] 建房成功后将返回房间快照直接写入面板本地状态，确保二维码和房间信息不依赖异步订阅而立即稳定显示
- [x] 删除任务前检查并释放关联同步房间、客户端 Socket、主控监听端口与面板快照，避免残留状态阻止下次建房
- [x] 在切换发音人、新建任务、进入另一任务、替换脚本和删除任务时按同步任务键统一释放不再匹配的同步房间资源与二维码快照
- [x] 移除同步诊断日志入口，并将关闭同步改为无条件停止房间、释放 Socket/端口、清空二维码和面板状态
- [x] 每次创建主控房间前无条件释放所有同步房间、客户端 Socket、主控监听端口和二维码快照，再创建新房间
- [ ] 排查并消除脚本同步任务键碰撞或缓存残留导致的部分文本无法建房，并将建房失败原因明确展示
- [x] 创建主控前清理同步模块缓存绑定与失效会话标识，不影响录音任务、脚本和发音人数据
- [x] 修复同步房间二维码与设备状态较长时关闭同步按钮在小屏设备越界不可达的问题
- [x] 在同步设备状态中隐藏本机主控，并为客户端提供与发音人一致的确认移除入口
- [x] 复核任务详情及主要页面的安全区、可滚动区域与长文字布局，避免小屏设备按钮或文字越界
- [x] 在主控同步录制界面显示可点选句子列表，点击后向所有在线客户端同步跳转到指定句子
- [x] 在主控同步录制准备阶段提供取消准备，并向所有在线客户端同步取消首端静音准备
- [x] 主控完成任务时停止所有设备当前操作，并让已连接客户端同步退出录制界面返回任务详情
- [x] 统一脚本导入与同步任务键对 LF、CRLF、CR 换行格式的规范化，避免特定文本无法创建主控房间
- [x] 为 LF、CRLF、CR 脚本导入与同步任务键增加回归测试
- [x] 客户端扫码加入后停留在任务详情，显示置灰的进入同步录制与可用关闭同步
- [x] 客户端录制界面锁定所有本地控制项和右上角句子列表，仅执行主控同步指令
- [x] 主控进入同步录制时驱动客户端进入录制界面，主控完成任务时驱动客户端返回任务详情
- [x] 修复主控同步录制的重复导航与返回栈嵌套，确保一次返回即可回到任务详情
- [x] 将音频文件名末尾 Unix 时间戳改为本地可读时间格式，保持既有文件名前缀不变
- [x] 核对并强化同步设备状态的实时推送与界面刷新机制
- [x] 制作主控录制页“进度条下、提示词上”的设备状态区预览，并根据反馈确认采用紧凑圆点方案
- [x] 在进度条与提示词之间按连接顺序显示紧凑设备状态圆点，绿色在线、灰色离线
- [x] 将任务详情同步面板的设备状态列表改为独立滚动区域，固定进入同步录制和关闭同步按钮
- [x] 在录制页设备状态圆点内显示名称首字符，并点击后展示完整设备名称
- [x] 将录制页设备圆点改为绿（已进入录制页）、黄闪（在线未进入录制页）、红（离线）的实时状态语义
- [x] 压缩同步面板主控地址、口令和设备状态间距，取消模块整体滚动并保证底部关键操作不越界
- [x] 主控完成同步任务后，客户端返回任务详情时保持多设备同步录音模块默认展开并可关闭
- [x] 设备离开录制界面但仍在线时立即回报 idle，使其他设备显示黄色闪烁状态
- [x] 参考 LiquidSlider.kt 重做字体滑块为单层轨道、扩大拖拽热区与按压放大滑块，消除底部多余叠层
- [x] 复核并修复主要页面在小屏设备上的安全区、长文字、列表滚动和关键按钮越界风险
- [x] 将朗读字体滑块范围扩展为 10–100，并保持实时预览与保存
- [x] 将朗读字体默认值改为 20，滑块范围调整为 15–50
- [x] 设备状态圆点点击后以完整可换行浮层显示设备名称，不截断且不越出屏幕
- [x] 将录制波形窗口调整为 120ms，并以每窗峰值替代 RMS/均值聚合
- [x] 将波形显示重采样改为仅取窗口峰值，并补充 120ms 峰值回归测试
- [x] 将录制峰值波形窗口调整为 200ms，并降低静音或背景噪声的显示幅度
- [x] 修复字体液态滑块最左端拖拽时向右回跳的问题，提升边界稳定性
- [x] 审查并优化录制回调、高频状态更新和音频资源释放，不改变功能或页面按键布局
- [x] 将字体滑块改为累计手势位移映射，拖到最左端后稳定锁定最小值不回跳
- [x] 同步录制仅校验在线客户端与句数一致性，忽略脚本名和内容差异
- [x] 主控无法进入同步录制时明确显示无客户端、离线或双方句数不一致的具体原因
- [x] 仅回退累计位移字体滑块到上一版本实现，保持同步句数准入和其他功能不变
- [x] 按参考参数重做字体液态滑块：静止细轨小色块，按压时白色玻璃滑块和扩展轨道平滑过渡
- [x] 回退到 1cdecd1，撤销最新液态字体滑块视觉重做
- [x] 评估百度网盘 Android SDK 在每句录音完成后自动上传到用户指定目录的可行性、授权与失败重试方案
- [x] 阅读百度网盘开放平台简介、接入流程与权限配额，补全逐句自动上传的审批和使用限制
- [x] 修复英文界面主要页面的长文本、按钮和图标在小屏设备上的溢出与裁切
- [x] 修复设置页当前录制配置中首端和尾端静音字段被吞噬的问题
